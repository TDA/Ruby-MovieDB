<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Movies List</title>
</head>
<body>
Hello, this is a movies list.
<ul>
  <!--Access the values as usual, but inside
  of output embedded tags -->
  <li><%= @movie.title %></li>
  <!--These are regular embedded tags, they dont output anything -->
  <% unless @movie.title.nil? %>
    <li>This will be output</li>
  <% end %>

  <!--combine both for max effect :D-->
  <% unless @movie.title.nil? %>
      <li><%= @movie.title %></li>
  <% end %>

  <% @movies.each do |movie| %>
      <li><%= movie.title %></li>
  <% end %>

</ul>
<a href="/movies/new">Add a Movie</a>
</body>
</html>